node_modules/
.env
*.log
# runtime artifacts (slides, analysis, learnings) live in CAROUSEL_HOME (/tmp/carousel)
runs/
analysis.json
learnings.json
slide-*.jpg
# ...except the persistent, repo-tracked learnings state used by cloud runs
!state/
!state/learnings.json
