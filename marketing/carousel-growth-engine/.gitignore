node_modules/
.env
*.log
# runtime artifacts (slides, analysis, learnings) live in CAROUSEL_HOME (/tmp/carousel)
runs/
analysis.json
learnings.json
slide-*.jpg
# ...except the persistent, repo-tracked state (learnings + last published
# carousel: slides, caption, post-info, run-report) used/archived by cloud runs
!state/
!state/**
